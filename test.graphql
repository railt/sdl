
type User
type Message

interface PaginatorInterface<T> implements PaginatorInterface<T> {
    first: T
}

type Paginator<T> {
    first: T
    count: Int!
}

type ExampleUnusedGenericType<X>

type Query {
    users: Paginator<User>!
    messages: Paginator<Message>!
}
